spec_file | 规则原文 | 规则类型 | 验证结果 | 证据
.trellis/spec/frontend/quality-guidelines.md | ValueNotifier+VLB 替代 setState 做局部重建（最小子树） | 性能约束 | 通过 | home_page.dart:114 `widget.tabIndex.removeListener(_maybeBuild)` + _LazyIndexedPage 按 index 门控仅重建自身子树
.trellis/spec/frontend/quality-guidelines.md | 禁止重复取消已取消的资源（批量不重复 cancel） | 流程约束 | 通过 | P1-F `cancelReminderForSchedule(task.id, cancelRepeats: false)`（home_page:331）跳过 21 次 repeat 取消循环，仅 2 次基础取消
.trellis/spec/frontend/quality-guidelines.md | dispose 中取消防抖/监听，避免销毁后访问已释放资源 | 并发约束 | 通过 | _LazyIndexedPageState.dispose（home_page:114）removeListener + _HomePageState 各订阅 dispose
.trellis/spec/frontend/quality-guidelines.md | 用户打开 App 体感 = 本地 I/O 延迟，绝不能 = 网络 RTT | 性能约束 | 通过 | _syncCloudPrefsAfterLoad（task_bloc:518）`if (DataBackendConfig.current != DataBackend.cloud) return;` 本地模式断网；_initSupabase 2s 超时不阻塞首帧
.trellis/spec/frontend/state-management.md | Bloc 用 Event→State 管理；drift 是唯一真源 | 数据一致性 | 通过 | 任务写路径本地 drift 提交；CloudSyncGateway 接口保留云端口子（LocalOnly no-op）
.trellis/spec/frontend/platform-compatibility.md | Flutter Web/native 分端 stub，签名同步 | 安全约束 | 通过 | notification_service_web.dart:90 cancelRepeats 签名与 io 同步（5749d13 web stub 兼容）
.trellis/spec/frontend/quality-guidelines.md | 通知防重复弹窗（缓存上次已通知状态） | 流程约束 | 需确认 | _onAppResume 30s 节流（_lastResumeLoadTime）防重复重载；但 `_lastRescheduleTime`/`_lastResumeLoadTime` 双字段是否冗余待人工确认
