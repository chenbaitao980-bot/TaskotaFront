# 行为契约（Phase 2 Q3 强制产物）— V3 独立便签窗重构
# 前置 gitnexus impact（已跑）：DesktopFloatingTabController upstream=LOW(4 direct)；风险见 .blast_radius。

## A. rankCandidates 分层规则（纯函数，flutter test 可测）
┌────────┬──────────────────────────────────────────────────────────┬──────────────────────────────────────────────┐
│ 契约   │ 输入（now=2026-08-03 12:00）                            │ 预期输出                                      │
├────────┼──────────────────────────────────────────────────────────┼──────────────────────────────────────────────┤
│ R1-C1  │ 层①小时级任务(单日非全天,start=+2d) + 层②跨天任务(start=+1d,due=+2d)│ 顺序=[小时级, 跨天]（层①优先，即使跨天时间更近）│
│ R1-C2  │ 层①内：待办(status0,start=now) + 进行中(status1,start=+1d)│ 顺序=[进行中, 待办]（层内进行中优先）          │
│ R1-C3  │ 池含无 startDate 任务(status0,无日期) + 有startDate任务   │ 无 startDate 任务被剔除，不在结果中            │
│ R1-C4  │ 层①内：start=+1d(upd100) + start=+1d(upd200) + start=+3d │ 顺序=[upd200, upd100, +3d]（距now最近→平局updAt最新）│
│ R1-C5  │ 全天任务(isAllDay=1,start=+1d) + 小时级任务(start=+2d)    │ 顺序=[小时级, 全天]（全天归层②靠后）           │
│ R1-C6  │ 跨天层内：进行中(start=+5d) + 待办(start=+2d)，均跨天      │ 顺序=[跨天进行中, 跨天待办]（层内进行中优先，时间其次）│
│ R1-C7  │ 空池                                                    │ 返回 []（候选为空 → 便签不显示）                │
└────────┴──────────────────────────────────────────────────────────┴──────────────────────────────────────────────┘

## B. 控制器生命周期（集成，build + 文本断言验证）
┌────────┬──────────────────────────────────────────────────────────┬──────────────────────────────────────────────┐
│ 契约   │ 输入                                                     │ 预期输出                                      │
├────────┼──────────────────────────────────────────────────────────┼──────────────────────────────────────────────┤
│ C1     │ handleCloseRequested（认证态+有候选）                    │ 创建/复用便签窗+推送摘要+隐藏主窗（不再 resize）│
│ C2     │ handleCloseRequested（无候选/未启用/dismissed）          │ 仅 hideToTray，不创建便签窗                    │
│ C3     │ restoreFullWindow(openTaskId:X)                          │ 写 pendingFocusTaskId=X + 隐藏便签窗 + show/focus 主窗 │
│ C4     │ closeFloatingTab()                                       │ 置 dismissedUntilRestore + 隐藏便签窗（主窗保持隐藏）│
│ C5     │ 二次 handleCloseRequested（便签窗已存在）                │ 复用既有窗，notifyTask 更新摘要，不重复创建      │
└────────┴──────────────────────────────────────────────────────────┴──────────────────────────────────────────────┘

## C. 便签窗（集成，build + 人工验证）
┌────────┬──────────────────────────────────────────────────────────┬──────────────────────────────────────────────┐
│ 契约   │ 输入                                                     │ 预期输出                                      │
├────────┼──────────────────────────────────────────────────────────┼──────────────────────────────────────────────┤
│ W1     │ 便签引擎启动                                             │ 透明无边框 360x112 置顶跳过任务栏右上角（无黑线）│
│ W2     │ 便签 onTap                                               │ 自隐 + 通道 showMain{openTaskId} → 主窗恢复定位  │
│ W3     │ 便签 onClose                                             │ 自隐 + 通道 dismissNote → 主窗保持隐藏           │
│ W4     │ 主窗→notifyTask                                          │ 便签引擎收到并更新摘要渲染                      │
└────────┴──────────────────────────────────────────────────────────┴──────────────────────────────────────────────┘

# 验证方式
# - R1 系列: flutter test test/floating_tab_controller_test.dart（纯函数断言）
# - B/C 系列: flutter analyze + flutter build windows --debug（编译级）+ 文本断言（handleCloseRequested 无 setSize/_enterFloatingMode 调用）
# - 便签窗渲染无法无头验证（依赖真实窗口）→ 交用户 release 构建实测，change_report 标注
