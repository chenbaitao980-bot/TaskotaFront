## 2026-05-24

### 全局 UI 重设计 "Claude Light" 完成
- **概念**: Claude Code UI 启发亮色主题，暖白基底 + 珊瑚橙主色
- **配色**: bg=#F5F5F0, card=#FFFFFF, primary=#C15F3C, text=#1A1A1A
- **字体**: Inter (正文) + Instrument Serif (标题) + JetBrains Mono (数字/时间)
- **修改文件**: 14 个文件 (pubspec.yaml + app_theme + main + 11 个页面)
- **构建结果**: `flutter build windows --release` 成功，ZIP 打包完成
- **新增依赖**: google_fonts: ^6.2.1

### 设计要点
- 卡片以纯白 + 轻微阴影替代原暗色毛玻璃效果
- 首页概览卡片顶部珊瑚色装饰条
- 日程项左侧优先级色竖条 + 白底
- 用户消息气泡浅珊瑚底(#FEF0EA)，AI 气泡白底阴影
- 底部导航纯白底 + 选中项圆点指示器
- AI 对话页绿色脉冲呼吸灯

### complex-project-workflow 技能脚本化改造
- **问题 1 解决（触发绕道）**: SKILL.md 顶部新增「⚠️ 入口强制检查」清单 + 4 条禁越指令，模型首条回复必须输出检查结果
- **问题 2 解决（token 过高）**: 内联模板迁到 5 个 Python 脚本，SKILL.md 从 1309 行精简到 392 行（精简 70%）
- **新建脚本 (5 个)**:
  - `generate-four-piece.py`: JSON/CLI 输入 → 生成四件套文件
  - `scan-openspec.py`: 关键词扫描 specs/changes/bugfix，输出结构化 JSON
  - `bootstrap-project.py`: 项目初始化（目录 + project.md + baseline spec + GitNexus）
  - `change-lifecycle.py`: change 创建/归档/列表/状态
  - `bugfix-manage.py`: bugfix 日志创建/更新/沉淀/检查
- **预计 token 节省** ~70%（每次会话 ~19,800 → ~5,850 tokens）
- **脚本位置**: `C:\Users\Administrator\.workbuddy\skills\complex-project-workflow\scripts\`

### GitNexus 调用修正（v2 更新）
- 发现 `NODE_OPTIONS=--use-system-ca` 环境变量冲突导致所有 gitnexus 命令失效
- 所有命令增加 `NODE_OPTIONS=""` 前缀
- GitNexus 提升为 token-saving 核心手段：编辑任何 symbol 前必须先跑 `impact`，只读命中文件
- 新增「GitNexus 统一调用方式」和「GitNexus 优先定位流程」区块
- 新增 `impact` 用法说明：target 必须用类名/函数名（非文件名）
- SKILL.md 增加 GitNexus 速查表条目

### 实施变更: ai-taskflow-planning-upgrade
- **动机**: AI 规划流程从 round-based 升级为 TaskFlow 式 diagnose/classify/ask/build/route 合同；统一中文提示；修复 AI 对话中分配映射保持父子关系
- **修改文件**:
  - `lib/services/ai_service.dart`: system prompt 完全重写，改为 TaskFlow 5 步流程 + 稳定标记 `[TABLE_BEGIN]/[TABLE_END]`、`[HIERARCHY_BEGIN]/[HIERARCHY_END]`
  - `lib/presentation/pages/ai_chat/ai_chat_page.dart`: 计划检测支持稳定标记、传递父-子关系的分配映射
  - `lib/presentation/pages/task/task_detail_page.dart`: 删除保护提示中文化+可关闭按钮
  - `regression-tests/cases/ai-taskflow-planning-upgrade.md`: 新增 8 个回归测试用例
- **四件套**: proposal/design/spec delta/tasks 齐全，8/9 实施项完成
- **检测**: flutter analyze 通过 (0 error/warning)；gitnexus detect-changes 执行
- **待用户验证**: 4 项功能验证

### Bug 修复: fix-ai-options-fallback (AI 拆分选项卡错误)
- **问题**: AI 拆分对话时第三轮"希望达到什么效果"的选项卡显示为时间选项（每天30分钟等）
- **根因**: `_generateSuggestions` 关键词匹配方案不可靠（bugfix_count=3 的反复bug）
- **修复方案**: 完全移除客户端关键词匹配回退，改为仅依赖 AI 输出的 `[OPTIONS:]` 结构化标记
- **修改文件**:
  - `lib/services/ai_service.dart`: system prompt 强化 `[OPTIONS:]` 必须规则，加入明确示例
  - `lib/presentation/pages/ai_chat/ai_chat_page.dart`: 移除 `_generateSuggestions`/`_canUseExplicitOptions`/`_shouldSuggestForQuestion`/`_isShortQuestion` 4 个方法；`_callAI` 建议逻辑简化为仅解析 `[OPTIONS:]`
- **检测**: flutter analyze 通过 (0 error/warning)；gitnexus detect-changes 执行
- **状态**: user_verification_pending，等待构建验证
  - 用户反馈调整：
    1. Bugfix spec 改为"历史经验参考"定位，不再作为硬性禁止规则。若再次触发说明策略需重新分析
    2. System prompt [OPTIONS:] 示例改为纯格式示范，提问内容和顺序由 AI 自由决定
  - **skill 调整**: complex-project-workflow SKILL.md 中 Step 0.5 BugFixSpecs 预读 / Bug修复规则计数门槛 / 归档沉淀 / 强制失败处理 全部从"禁止做法硬规则"改为"历史教训参考"；bugfix-manage.py 脚本增加 --lessons 参数兼容旧 --forbidden；沉淀模板表头从"禁止做法"改为"复盘教训（供参考）"
  - **再次调整**: AI 有时不输出 [OPTIONS:]，导致完全无选项卡。添加安全通用回退（'好的，继续', '让我想想', '换个方向'），
    仅在 AI 忘加 [OPTIONS:] 且消息明显是问题时激活。不涉及具体内容关键词，不会匹配错误分支。
  - **最终确定方案**: 用户指出固定模板回退也是客户端猜测，违背原则。
    移除所有回退（包括通用模板），改为强制强化 AI prompt 输出 [OPTIONS:]。
    prompt 增加自检清单和末尾重申确认，确保 AI 每次提问都带上 [OPTIONS:]。
